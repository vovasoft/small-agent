请根据以下规范，生成一个数据处理规则的 JSON 数组 `ruleContent`。

**规则链说明**：
数据在处理链中流动。第一个操作的 sourceField 通常是原始数据字段（如 "data"），后续操作的 sourceField 是前一个操作的 resultField。请确保字段名前后连贯。

**可用操作类型 (`type`)**: `FILTER`, `CONDITIONAL`, `AGGREGATE`, `GROUP_BY`, `SORT`, `ADDTAG`。

**可用操作符 (`operator`)**: `EQUALS`, `NOT_EQUALS`, `CONTAINS`, `GREATER_THAN`, `LESS_THAN`, `GREATER_THAN_OR_EQUAL`, `LESS_THAN_OR_EQUAL`, `REGEX_MATCH`。

**生成要求**：
请生成一个满足以下自然语言描述的规则链：
【这里描述你的要求】

**抽象结构参考（请遵循此结构生成具体 JSON）**：
```json
[
  {
    "type": "ADDTAG | FILTER | ...",
    "sourceField": "...",
    "resultField": "...",
    // 其他与类型相关的参数...
  }
  // ... 可以串联多个步骤
]
```

**请先生成规则的自然语言描述，然后输出对应的、格式正确且完整的 JSON 代码。**